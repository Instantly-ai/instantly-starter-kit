# Copy to .env and fill in. Never commit real secrets.
INSTANTLY_API_KEY=sk_...
# How many leads to enrich per run (spends credits).
LIMIT=100
# Default run is preview-only (free). Set to 1 to actually enrich (spends credits + creates a list).
CONFIRM_ENRICH=0
