INSTANTLY_API_KEY=sk_...
# Optional: act as an agency sub-workspace (requires an admin-workspace key)
# AS_WORKSPACE=<sub_workspace_id>
# Optional: domains to DNS-verify in `incident` (SPF/DMARC), comma-separated
# DOMAINS=acme.com,acme-sales.com
