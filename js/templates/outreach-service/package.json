{
  "name": "outreach-service",
  "private": true,
  "type": "module",
  "description": "Hero template: BYO audience → list → enrich + verify → campaign → launch → reply webhook → status.",
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "start": "tsx src/index.ts",
    "dev": "tsx watch src/index.ts"
  },
  "dependencies": {
    "@instantly-ai/sdk": "file:../../sdk"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.9.2"
  }
}
